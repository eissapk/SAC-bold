/** @type {import('next').NextConfig} */

const nextConfig = {
  // output: 'standalone',
  // i18n: {
  //     locales: ["default", "en", "ar"],
  //     defaultLocale: "en",
  //     localeDetection: false,
  // },
}

module.exports = nextConfig
